# Supply-chain hardening: refuse any package version published less than 7 days
# ago, so freshly-published malicious releases age out before we ever install
# them. Enforced at install time. Requires npm >= 11.10.0.
min-release-age=7
